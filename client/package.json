{
  "name": "socket-io-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cd .. && rm -rf dist && mkdir -p dist/client && mkdir -p dist/server && cd client && cp -a build/. ../dist/client/ && rm -rf build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "generate-component": "npx generate-react-cli component"
  },
  "dependencies": {
    "@types/node": "^17.0.21",
    "@types/react": "^17.0.40",
    "@types/react-dom": "^17.0.13",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-json-view": "^1.21.3",
    "react-scripts": "5.0.0",
    "socket.io-client": "^4.4.1",
    "sql-formatter": "^4.0.2",
    "typescript": "^4.6.2",
    "web-vitals": "^2.1.4"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
